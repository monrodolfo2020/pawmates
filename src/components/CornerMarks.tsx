// The old "blueprint" identity decorated cards/buttons with a crosshair
// "+" mark at each corner — a technical-wireframe look that doesn't fit
// PawMates' current rounded, colorful identity (see theme/vividTokens.ts).
// Left as a no-op (not deleted) so every existing `blueprint` prop across
// the app keeps compiling instead of needing a matching edit at each
// call site.
export default function CornerMarks() {
  return null;
}
